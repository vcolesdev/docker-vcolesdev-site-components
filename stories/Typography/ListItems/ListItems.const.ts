/**
 * The default color for the item's icon.
 * @const DEFAULT_ICON_COLOR
 */
export const DEFAULT_ICON_COLOR = "currentColor";

/**
 * The default TablerIcon name for the item's icon.
 * @const DEFAULT_ICON_NAME
 */
export const DEFAULT_ICON_NAME = "IconCheck";

/**
 * The default size for the item's icon.
 * @const DEFAULT_ICON_SIZE
 */
export const DEFAULT_ICON_SIZE = 16;

/**
 * The default stroke for the item's icon.
 * @const DEFAULT_ICON_STROKE
 */
export const DEFAULT_ICON_STROKE = 3;

/**
 * Default list items for the ListItems component
 * @const DEFAULT_LIST_ITEMS
 */
export const DEFAULT_LIST_ITEMS = [
  "NextJS TypeScript CRUD app that manages clients' information in a simple dashboard interface.",
  "A simple API that converts RSS feeds to JSON format and downloads the results.",
  "A custom Kanban board built with React and Vite. Features drag and drop functionality and more.",
  "Custom components for a personal project, Mizu Components. Built with React and Storybook.",
];
