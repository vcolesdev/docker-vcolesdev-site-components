{/* SitePalette.mdx */}

import { swatches } from "../src/swatches";

import { ColorSwatch } from "@/stories/ColorSwatch/ColorSwatch";

# Site Palette

The site colors are defined in the `src/swatches.ts` file. Additionally, the colors are defined as CSS variables in `src/tailwind.css` and its configuration and throughout the site.

## Neutrals

<ul>
    <li>White</li>
    <li>Black</li>
    <li>Neutral</li>
</ul>

<div className="grid grid-cols-4 gap-10 py-10">
    <ColorSwatch color={swatches.white} size="lg" />
    <ColorSwatch color={swatches.black} size="lg" />
    <ColorSwatch color={swatches.neutral} size="lg" />
</div>

## Site

<ul>
    <li>Melon</li>
    <li>Baby Pink</li>
    <li>Rose</li>
    <li>Rose - Dark</li>
    <li>Sky</li>
    <li>Baby Blue</li>
    <li>Charcoal Blue</li>
    <li>Freshwater Blue</li>
    <li>Deep Brown</li>
    <li>Tree Bark</li>
</ul>

<div className="grid grid-cols-4 gap-10 py-10">
    {Object.keys(swatches.site).map((key) => (
        <ColorSwatch key={key} name={key} color={swatches.site[key]} size="lg" />
    ))}
</div>

## States

<ul>
  <li>Success</li>
  <li>Warning</li>
  <li>Danger</li>
</ul>

<div className="grid grid-cols-4 gap-10 py-10">
    {Object.keys(swatches.states).map((key) => (
        <ColorSwatch key={key} name={key} color={swatches.states[key]} size="lg" />
    ))}
</div>

## Actions

<ul>
    <li>Primary</li>
    <li>Primary - Dark</li>
</ul>

<div className="grid grid-cols-4 gap-10 py-10">
    {Object.keys(swatches.actions).map((key) => (
        <ColorSwatch key={key} name={key} color={swatches.actions[key]} size="lg" />
    ))}
</div>

## Backgrounds

<ul>
    <li>Light</li>
    <li>Light - Elevated</li>
    <li>Dark</li>
    <li>Dark - Elevated</li>
</ul>

<div className="grid grid-cols-4 gap-10 py-10">
    {Object.keys(swatches.bg).map((key) => (
        <ColorSwatch key={key} name={key} color={swatches.bg[key]} size="lg" />
    ))}
</div>